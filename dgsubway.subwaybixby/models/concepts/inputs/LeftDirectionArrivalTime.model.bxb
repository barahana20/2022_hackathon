structure (LeftDirectionArrivalTime) {
  description ("왼쪽방향 열차도착 시간")
  property (hour){
    type(Hour)
    min(Required) max(One)
  }
  // 시간을 따로 객체(model) 로 만들어서 처리함.
  property (minute){
    type(Minute)
    min (Required) max (One)
  }
}
