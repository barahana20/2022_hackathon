structure (SubwayResult4){
  description (~~역 첫차 or ~~역 막차)
  // 1개 방면 첫차 or 막차 시간 알려주기
  property (directionFirstTime) {
    type (LeftFirstTime)
    min (Required) max (One)
  }
  property (directionLastTime) {
    type (LeftLastTime)
    min (Required) max (One)
  }
}
