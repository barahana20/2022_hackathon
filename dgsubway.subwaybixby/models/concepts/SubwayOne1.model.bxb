structure (SubwayOne1) {
  description ("~~역 (가르쳐 줘)!")
  // 양 방향 도착정보 1개씩(몇분 남았는지), with 첫차막차 시간 대답으로 띄우기
  property (toSubwayTime) {
    type (ToSubwayTime)
    min (Required) max (One)
  }
  property (toLeftTime) {
    type (ToLeftTime)
    min (Optional) max (One)
  }
  property (fromSubwayTime) {
    type (FromSubwayTime)
    min (Required) max (One)
  }
  property (fromLeftTime) {
    type (FromLeftTime)
    min (Optional) max (One)
  }
}
